services:
  - type: web
    name: whatsapp-backend
    env: node
    buildCommand: ./render-build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true